pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "Hello World"'
                sh '''
                    echo "Turtle head steps works too"
                    ls -lah
                '''
            }
        }
    }
}
