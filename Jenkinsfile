pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "Hello World"'
                sh '''
                    echo "you look like bono"
                    ls -lah
                '''
            }
        }
    }
}
